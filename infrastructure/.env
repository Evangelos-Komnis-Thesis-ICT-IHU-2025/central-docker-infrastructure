# Shared settings
TZ=Europe/Athens
ELASTIC_VERSION=8.15.3

# MinIO credentials (dev only)
MINIO_ROOT_USER=scormadmin
MINIO_ROOT_PASSWORD=scormadmin123

# Host docker group id used by Jenkins for /var/run/docker.sock access
# Discover with: getent group docker | cut -d: -f3
DOCKER_GID=998
